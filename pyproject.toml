[project]
name = "german-credit"
version = "0.1.0"
description = "Case study for data science using a famous dataset"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
german-credit = "german_credit:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
